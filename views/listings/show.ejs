<% layout("/layouts/boilerplate") %>
<div class="row">
    <div class="col-8 offset-3 mt-3">
        <h3><%= listing.name %></h3>
    </div>
    <div class="card col-6 offset-3 show-card listing-card">
        <img src="<%= listing.image.url %>" class="card-img-top show-img" alt="listing_image">
        <div class="card-body">
          <p class="card-text"><b>Student Description -</b><%=listing.description%></p>
              <p class="card-text"><b>Enrollment no -</b><%=listing.enrollment_no%></p>
              <p class="card-text"><b>Age -</b><%=listing.age%></p>
              <p class="card-text"><b>Address -</b><%=listing.address%></p>
              <p class="card-text"><b>Current CGPA -</b><%=listing.cgpa%></p>
      </div>
</div>
<br>
<%if(currUser && listing.owner._id.equals(currUser._id)){%>
    <div class="btns">
        <a href="/listings/<%=listing._id%>/edit" class="btn btn-dark col-1 offset-3 edit-btn">Edit</a>
        <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
            <button class="btn btn-dark offset-5">Delete</button>
        </form>
    </div>
    <%}%>